# 🟣 NVG Landing — Digital Systems for Automation, Design & Growth

Футуристичный одностраничный лендинг для digital-агентства NVG с тёмной неоновой палитрой, плавными анимациями и кастомным smooth scroll.

## 🎨 Дизайн-концепция

**Визуальный стиль:** Dark Neon Cyber Minimalism
- Тёмная палитра (#0A0014) с фиолетовыми акцентами
- Glassmorphism карточки с blur-эффектами
- Неоновые свечения и glow-тени
- Плавный cinematic scroll (Lenis + GSAP)
- Кастомный курсор (desktop only)

## 🚀 Технологии

- **React 18** + **Vite** — быстрая сборка и HMR
- **GSAP** + **ScrollTrigger** — профессиональные анимации
- **Lenis** — smooth scroll с инерцией
- **Framer Motion** — декларативные анимации компонентов
- **CSS Custom Properties** — дизайн-токены и темы

## 📁 Структура проекта

```
nvg-landing/
├── src/
│   ├── components/
│   │   └── sections/          # Секции сайта
│   │       ├── Preloader.jsx
│   │       ├── Hero.jsx
│   │       ├── Services.jsx
│   │       ├── Portfolio.jsx
│   │       ├── Pricing.jsx
│   │       ├── Contact.jsx
│   │       └── Outro.jsx
│   ├── hooks/
│   │   └── useLenis.js        # Lenis smooth scroll
│   ├── utils/
│   │   └── phoneValidation.js # Валидация телефона Молдовы
│   ├── services/
│   │   └── notify.js          # Отправка уведомлений
│   ├── App.jsx
│   ├── main.jsx
│   └── styles.css             # Глобальные стили
├── package.json
└── vite.config.js
```

## 🛠 Установка и запуск

### 1. Установить зависимости

```bash
npm install
```

### 2. Запустить dev-сервер

```bash
npm run dev
```

Откроется `http://localhost:5173`

### 3. Собрать для production

```bash
npm run build
```

Результат в папке `dist/`

### 4. Превью production сборки

```bash
npm run preview
```

## ⚙️ Конфигурация

### Переменные окружения

Создайте `.env` в корне проекта:

```env
# Telegram Bot (опционально)
VITE_TELEGRAM_BOT_TOKEN=your_bot_token
VITE_TELEGRAM_CHAT_ID=your_chat_id
```

### Настройка уведомлений

По умолчанию форма использует заглушку в `src/services/notify.js`.

**Для подключения реального бэкенда:**

1. Замените `notify()` функцию на реальный API endpoint
2. Или настройте Telegram Bot (см. `notifyTelegram()`)

```javascript
// Пример с API
export async function notify(payload) {
  const response = await fetch('/api/lead', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(payload),
  });
  
  return await response.json();
}
```

## 🎯 Основные возможности

### ✅ Preloader
- Анимированный логотип с pixel build-in
- Градиентный прогресс-бар
- Плавный fade-out

### ✅ Hero Section
- Заголовок с glow-эффектом
- Анимированный 3D-подобный фон
- Параллакс при движении мыши
- CTA кнопки с навигацией

### ✅ Services
- 3 карточки с иконками
- Glassmorphism стиль
- Hover-анимации и glow

### ✅ Portfolio
- Grid из 6 кейсов
- Изображения с Unsplash
- Hover overlay с призывом к действию
- Адаптивная сетка

### ✅ Pricing
- 3 тарифных плана
- Выделенный "рекомендуемый" план
- Список features с галочками

### ✅ Contact Form
- Валидация телефона Молдовы (+373)
- Автоформатирование при вводе
- Состояния: loading, success, error
- Доступность (ARIA-атрибуты)

### ✅ Outro
- Логотип с pulse-анимацией
- Контактная информация

### ✅ Дополнительно
- Кастомный неоновый курсор
- Scroll-to-top кнопка
- Smooth scroll с Lenis
- Все анимации на GSAP + Framer Motion

## 📱 Адаптивность

- **Desktop** (≥1280px) — полная версия
- **Tablet** (768–1279px) — 2 колонки
- **Mobile** (≤767px) — 1 колонка, оптимизированные анимации

## ♿️ Доступность

- Семантические HTML-теги
- ARIA-атрибуты для форм
- Keyboard navigation
- Focus-visible стили
- `prefers-reduced-motion` поддержка

## 🎨 Дизайн-токены

### Цвета

| Название      | HEX       | Применение |
|---------------|-----------|------------|
| `--bg`        | `#0A0014` | Основной фон |
| `--accent`    | `#7A33FF` | Акцент |
| `--glow`      | `#CBA3FF` | Свечения |
| `--text`      | `#EDEDED` | Текст |
| `--error`     | `#FF4F81` | Ошибки |
| `--success`   | `#2AE6B8` | Успех |

### Типографика

- **Заголовки:** Space Grotesk, 600–700 weight
- **Текст:** Inter, 400–500 weight
- **Responsive:** clamp() для всех размеров

## 🔧 Кастомизация

### Добавить новый проект в Portfolio

Отредактируйте массив в `src/components/sections/Portfolio.jsx`:

```javascript
const projects = [
  {
    title: 'Название проекта',
    tag: 'Тег',
    image: 'https://your-image-url.jpg'
  },
  // ... остальные
];
```

### Изменить тарифы

Отредактируйте `src/components/sections/Pricing.jsx`:

```javascript
const plans = [
  {
    name: 'Название',
    price: '$999',
    features: ['Фича 1', 'Фича 2'],
    recommended: false,
  },
];
```

### Изменить цветовую схему

Отредактируйте CSS-переменные в `src/styles.css`:

```css
:root {
  --bg: #yourcolor;
  --accent: #yourcolor;
  --glow: #yourcolor;
}
```

## 📊 Performance

- **Lighthouse Score:** 90+ Performance
- **Lazy loading** для изображений
- **Code splitting** через Vite
- **CSS минификация**
- **Tree shaking** для неиспользуемого кода

## 🚀 Деплой

### Vercel

```bash
vercel --prod
```

### Netlify

```bash
netlify deploy --prod
```

### Manual

1. `npm run build`
2. Загрузите папку `dist/` на хостинг

## 📝 TODO / Roadmap

- [ ] Интеграция с реальным API
- [ ] Telegram Bot для уведомлений
- [ ] Google Analytics / Yandex Metrika
- [ ] Добавить 3D-сцену через Three.js (опционально)
- [ ] Мультиязычность (EN/RU)
- [ ] Больше кейсов в Portfolio
- [ ] Блог-секция
- [ ] Темная/светлая тема

## 📄 Лицензия

MIT — свободное использование для коммерческих и личных проектов.

## 💬 Контакты

- **Email:** hello@nvg.digital
- **Telegram:** @nvgdigital

---

**Made with 💜 by NVG**
